name: shared
description: Shared utilities, constants, configurations, and common functionality used across all packages
version: 0.0.1
homepage: null
resolution: workspace

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.10.5
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter: null
