name: data
description: Data layer implementing repository interfaces with Firebase datasources and data models
version: 0.0.1
homepage: null
resolution: workspace

environment:
  sdk: ^3.10.7
  flutter: ">=3.10.0"

dependencies:
  domain:
    path: ../domain
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.10.5
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter: null
